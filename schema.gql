# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# The information for a job ticket
type Job {
  id: ID!
  customer: String

  # The address of the job
  address: String!

  # The primary phone number to contact for the job
  phone: String
  user: User!
}

# Editable job data
input JobInput {
  customer: String
}

type Mutation {
  addJob(
    # Add a new document to the job collection.
    data: JobInput!
  ): Job!
  deleteJob(
    # The ID of the document being deleted in the job collection
    id: String!
  ): Job!
  editJob(
    # Update a document in the job collection.
    data: JobInput!

    # The ID of the document in the job collection
    id: String!
  ): Job!
  addUser(
    # Add a new document to the user collection.
    data: UserInput!
  ): User!
  deleteUser(
    # The ID of the document being deleted in the user collection
    id: String!
  ): User!
  editUser(
    # Update a document in the user collection.
    data: UserInput!

    # The ID of the document in the user collection
    id: String!
  ): User!
}

type Query {
  # Get a specific job document from the collection.
  job(id: String!): Job

  # Get a list of job documents from the collection.
  jobs: [Job!]

  # Get a specific user document from the collection.
  user(id: String!): User

  # Get a list of user documents from the collection.
  users: [User!]
}

# The information for a user
type User {
  id: ID!
  name: String!
  email: String!
  jobs: [Job!]!
}

# Editable user data
input UserInput {
  name: String
}
